import '@testing-library/jest-dom';

describe('Step', () => {
  // it('should raise an error is Step is used outside stepper', () => {
  //   expect(() => render(<Step />)).toThrow(
  //     'Step must be used within a Stepper'
  //   );
  // });
  // it('should render the step component', () => {
  //   const {} = render(
  //     <Stepper currentStep={1}>
  //       <Step>A</Step>
  //       <Step>B</Step>
  //     </Stepper>
  //   );
  //   expect('A').toBeInTheDocument();
  // });
});
