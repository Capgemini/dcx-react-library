import '@testing-library/jest-dom';

describe('ListItem', () => {
  // for List component
  it('should render', () => {});

  it('should render with the dcx-list className when no className Provided', () => {});
  it('should be able to render the dcx-list and the user specific className', () => {});
  it('should be able to pass itemClassName prop', () => {});
  it('should be able to pass type prop with either ordered or unordered value', () => {});
  it('should be able to pass some extra properties', () => {});

  //   for nested ListItem component
  it('should render with the dcx-list-item className when no className Provided', () => {});
  it('should be able to render the dcx-list-item and the user specific className', () => {});
  it('should be able to pass value prop', () => {});
  it('should be able to pass some extra properties', () => {});
});
