import {
  Meta,
  Story,
  Canvas,
  Props,
  ArgTypes,
} from '@storybook/addon-docs/blocks';

import * as InsertTextStories from './UnStyled.stories';

<Meta title="DCXLibrary/Form/InsertText/Documentation" />

# InsertText

With InsertText component An Input component ready to use in your form with support for
InsertText.

InsertText is UI/UX agnostic so you need to provide your style to have the look and feel you prefer.

An example with all the available properties is:

When you import the InsertText component without providing any className or style associated
will looks as following:

<Canvas of={InsertTextStories.Unstyled} />

A more complex example with all the possible properties is:

```js
<InsertText value="insert text" id="user-defined-id" props={{}} />
```

Below a list of all the available properties:

<ArgTypes of={InsertTextStories} />
