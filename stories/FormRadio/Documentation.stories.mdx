import { Meta, Story, Canvas, Props } from '@storybook/addon-docs/blocks';
import { FormRadio } from '../../src/formRadio/FormRadio';
import '../style.css';

<Meta
  title="DCXLibrary/Form/Radio/documentation"
  component={FormRadio}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

# FormRadio

A radio component ready to use in your form.

FormRadio is UI/UX agnostic so you need to provide your style to have the look and feel you prefer.

## Usage

Use the radio component when users can only select one option from a list.

An example with all the available properties is:

```js
<FormRadio
  label="radio-label"
  value={value}
  ariaLabel="aria-label"
  ariaDescribedBy="aria-described-by"
  conditional={{
    name: 'conditional-reveal',
    label: 'label',
    type: 'text',
    className: 'classes',
    groupClassName: 'group-classes',
    id: 'conditional-id',
    inputClassName: 'input-classes',
    inputId: 'input-id',
  }}
  disabled={true}
  hint={{
    text: 'hint text',
    classes: 'class names',
    id: 'hint-id',
  }}
  id="radio-id"
  inputProps={{
    className: 'class-name',
    onChange: handleChange,
  }}
  itemProps={{
    className: 'class-name',
  }}
  labelProps={{
    style: { display: 'flex' },
  }}
  name="radio-name"
  onChange={handleChange}
  selected={true}
/>
```

<Props of={FormRadio} />
