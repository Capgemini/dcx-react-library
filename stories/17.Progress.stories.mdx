import { Meta, Story, Canvas, Props } from '@storybook/addon-docs/blocks';
import { Progress } from '../src/progress/Progress';

<Meta title="DCXLibrary/Progress" component={Progress} />

### Examples of 'un-styled' Progress
<Canvas>
    <Story name="indeterminate">
        {() => {
            return (
                <Progress
                    label="Progress"
                    max={100}
                />
            )
        }}
    </Story>
</Canvas>

<Canvas>
    <Story name="determinate">
        {() => {
            return (
                <Progress
                    label="Progress"
                    max={100}
                    value={80}
                />
            )
        }}
    </Story>
</Canvas>

<Props of={Progress} />