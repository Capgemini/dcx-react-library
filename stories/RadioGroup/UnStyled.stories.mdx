import { Meta, Story, Canvas, Props } from '@storybook/addon-docs/blocks';
import { RadioGroup } from '../../src/radioGroup/RadioGroup';
import '../style.css';

<Meta title="DCXLibrary/Form/RadioGroup/Unstyled" component={RadioGroup} />

### An example of 'un-styled' RadioGroup of radio buttons

<Canvas>
  <Story name="un-styled-radios">
    {() => {
      const [selected, setSelected] = React.useState('');
      const handleChange = event => setSelected(event.currentTarget.value);
      return (
        <RadioGroup
          name="un-styled-radio"
          legend={{
            text:
              'Heading text customisable by a given priority of type number between 1 to 6 (inclusive)',
            heading: {
              priority: 2,
            },
          }}
          items={[
            {
              label: 'item-label',
              value: 'yes',
              id: 'first',
            },
            {
              label: 'item-label',
              value: 'no',
              id: 'second',
            },
          ]}
          onChange={handleChange}
        />
      );
    }}
    />
  </Story>
</Canvas>

### An example of 'un-styled' RadioGroup of radio buttons

<Canvas>
  <Story name="un-styled-radios-with-strings">
    {() => {
      const [selected, setSelected] = React.useState('');
      const handleChange = event => setSelected(event.currentTarget.value);
      return (
        <RadioGroup
          name="un-styled-radios-with-strings"
          items={['yes','no']}
          onChange={handleChange}
        />
      );
    }}
    />
  </Story>
</Canvas>

# Error

<Canvas>
  <Story name="error">
    {() => {
      const [selected, setSelected] = React.useState('');
      const handleChange = event => setSelected(event.currentTarget.value);
      return (
        <RadioGroup
          name="name-of-group"
          items={[
            {
              label: 'item-label',
              value: 'yes',
              id: 'first',
            },
          ]}
          onChange={handleChange}
        />
      );
    }}
    />
  </Story>
</Canvas>

# Properties

<Props of={RadioGroup} />
