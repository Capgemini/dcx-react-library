import { Meta, Story, Canvas, Props } from '@storybook/addon-docs/blocks';
import { FormDate } from '../src/formDate/FormDate';
import './style.css';

<Meta title="DCXLibrary/Form/date" component={FormDate} />

### Examples of basic Form Date

<Canvas>
  <Story name="basic">
    {() => {
      return <FormDate dateFormat="dd/mm/yyyy" />;
    }}
  </Story>
</Canvas>

### Examples Form Date with custom label

<Canvas>
  <Story name="custom-label">
    {() => {
      return (
        <FormDate
          dateFormat="dd/mm/yyyy"
          yearProps={{
            classNameLabel: 'yearLabel',
            customLabel: <h3>Year</h3>,
          }}
          monthProps={{
            classNameLabel: 'yearLabel',
            customLabel: <h3>Month</h3>,
          }}
          dayProps={{
            classNameLabel: 'yearLabel',
            customLabel: <h3>Day</h3>,
          }}
        />
      );
    }}
  </Story>
</Canvas>

### Examples Form Date with custom label different date format

In the following example we specified as format: `yyyy/mm/dd`

<Canvas>
  <Story name="different-date-format">
    {() => {
      return (
        <FormDate
          dateFormat="yyyy/mm/dd"
          yearProps={{
            classNameLabel: 'yearLabel',
            customLabel: <h3>Year</h3>,
          }}
          monthProps={{
            classNameLabel: 'yearLabel',
            customLabel: <h3>Month</h3>,
          }}
          dayProps={{
            classNameLabel: 'yearLabel',
            customLabel: <h3>Day</h3>,
          }}
        />
      );
    }}
  </Story>
</Canvas>

<Props of={FormDate} />
