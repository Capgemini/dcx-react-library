import { Meta, Story, Canvas, Props } from '@storybook/addon-docs/blocks';
import { FormGroup } from '../../src/formGroup/FormGroup';
import FormGroupLive from '../liveEdit/FormGroupLive';

<Meta
  title="DCXLibrary/Form/Group/live"
  component={FormGroup}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

# Form Group

In the live editor you can play with all the available properties, change the look and feel and interact with the component

<Canvas>
  <Story name="live">
    <FormGroupLive />
  </Story>
</Canvas>

# Properties

below are described the list of all available properties. the one marked with (\*) are mandatory the other instead are optional.

<Props of={FormGroup} />
