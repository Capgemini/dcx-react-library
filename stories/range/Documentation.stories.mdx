import { Meta, Story, Canvas, Props } from '@storybook/addon-docs/blocks';
import { Range } from '../../src/range/Range';

<Meta
    title="DCXLibrary/Form/Range/documentation"
    component={Range}
    parameters={{
        viewMode: 'docs',
        previewTabs: {
            canvas: { hidden: true },
        },
    }}
/>

# Range

Range component ready to use in your UI

Range is UI/UX agnostic so you need to provide your style to have the look
and feel you prefer.

## Usage

Use the range component when users need to input based on a range of values

An example with all the available properties is:

```js
<Range min={0} max={100} value={75}/>
```

<Props of={Range} />
